# Пульт оркестра — зависимости.
# Трекер (cli.py status) работает на чистом Python, без этого пакета.
# Пакет нужен только для Telegram-бота (src/bot.py).
python-telegram-bot==22.5
# Конвертация черновиков .md → .docx перед отправкой в чат (через ../.md_to_docx.py).
python-docx
# Сборка .xlsx из таблицы сравнения конкурентов (src/sheet_export.py) → грузится как Google Sheet.
openpyxl
# Необязательно: авто-выгрузка готовых черновиков на Google Диск как Google Doc
# (src/drive_upload.py). Нужно, только если задан GOOGLE_OAUTH_TOKEN_FILE.
google-api-python-client
google-auth
# Только для РАЗОВОЙ авторизации (scripts/gdrive_authorize.py, на машине с браузером).
# Боту в проде не нужен — ставить вручную при настройке Google Docs.
# google-auth-oauthlib
